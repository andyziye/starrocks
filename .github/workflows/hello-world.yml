name: Test WorkFlowss

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    branches:
      - main
      - pipeline
      - target
  push:
    types:
      - opened
      - reopened
      - synchronize
    branches:
      - main
      - pipeline
      - target

jobs:

  TEST:
    runs-on: self-hosted
    steps:
      - name: TEST INFO
        run: |
          echo ${{github.event.pull_request.body}}

