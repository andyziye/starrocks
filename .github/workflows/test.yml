name: TEST upload

on:
  pull_request:
    types:
      - opened
      - synchronize

permissions:
  checks: write
  actions: write
  contents: write
  deployments: write
  discussions: write
  issues: write
  packages: write
  pages: write
  pull-requests: write
  repository-projects: write
  security-events: write
  statuses: write

jobs:
  job1:
    runs-on: ubuntu-latest
    name: job1
    steps:
      - run: echo 1

  job2:
    runs-on: ubuntu-latest
      if: !success()
      needs: job1
      name: job2
      steps:
        - run: echo 1

  job3:
    runs-on: ubuntu-latest
    needs: job2
    if: !success()
    name: job2
    steps:
      - run: echo 1