name: CI COMMENT TOOLS

on:
  issue_comment:
    types: [ created ]

permissions:
  checks: write
  actions: write
  contents: write
  deployments: write
  discussions: write
  issues: write
  packages: write
  pages: write
  pull-requests: write
  repository-projects: write
  security-events: write
  statuses: write

jobs:

  close1:
    runs-on: ubuntu-latest
    if: >
      ${{ github.event.issue.pull_request }} &&
      (contains(github.event.comment.body, 'close') ||
      contains(github.event.comment.body, 'xclose'))

    env:
      PR_NUMBER: ${{ github.event.issue.number }}
      

    steps:
      - name: CLOSE
        run: |
          echo "YES"
          cat ${GITHUB_EVENT_PATH}

  close2:
    runs-on: ubuntu-latest
    if: >
      contains(github.event.comment.body, 'close')

    env:
      PR_NUMBER: ${{ github.event.issue.number }}
      

    steps:
      - name: CLOSE
        run: |
          echo "YES"
          cat ${GITHUB_EVENT_PATH}

  close3:
    runs-on: ubuntu-latest
    if: contains(github.event.comment.body, 'xclose')

    env:
      PR_NUMBER: ${{ github.event.issue.number }}
      

    steps:
      - name: CLOSE
        run: |
          echo "YES"
          cat ${GITHUB_EVENT_PATH}
